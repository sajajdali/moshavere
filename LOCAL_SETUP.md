# اجرای محلی

پروژه با PHP 8.3 نصب‌شده در Homebrew و MySQL روشن XAMPP اجرا می‌شود.
برای اجرای مجدد، فایل `start-local.command` را اجرا کنید یا در پوشه پروژه دستور زیر را بزنید:

```sh
./start-local.command
```

- سایت: http://nobat1.test:8084
- ورود مدیریت سایت: http://nobat1.test:8084/shemiranWebLogin
- ورود پنل مرکزی: http://central.test:8084/centralLogin

این دامنه‌ها از قبل در `/etc/hosts` به 127.0.0.1 متصل بودند. پورت 8084 الزامی است.
دیتابیس مرکزی `appointment_v4_local` و دیتابیس سایت `appointmentv4_local_v4` است.
تنظیمات خصوصی در `.env` قرار دارند. `CENTRAL_DOMAINS=central.test` مسیرهای مرکزی را محلی نگه می‌دارد.
ورودهای بالا مسیرهای پشتیبانی موجود پروژه هستند و در اجرای محلی به رمز نیاز ندارند.

وابستگی‌های Composer نصب، تمام ماژول‌ها فعال، migrationها و seedهای سایت اجرا و فایل‌های اصلی Vite ساخته شدند. فایل‌های جاافتاده منو، تقویم و SweetAlert از منابع ماژول در public کپی شدند و favicon پیش‌فرض منتشر شد.

صفحه اصلی و پنل‌های مرکزی و سایت با HTTP 200 بررسی شدند. اجرای تست‌های موجود: ۴ موفق و ۷ ناموفق (انتظار قدیمی نسخه URL فایل‌ها، factory حذف‌شده و فرض صفحه اصلی روی دامنه مرکزی). `.env.testing` محلی از SQLite حافظه‌ای استفاده می‌کند تا تست‌ها دیتابیس نصب را تغییر ندهند.

پیامک، پرداخت و Firebase نیازمند کلیدهای معتبر سرویس‌های خود هستند و فعال‌سازی عملیاتی نشده‌اند. داده‌های واقعی پزشکان، نوبت‌ها، استان‌ها و تقویم تعطیلات همراه مخزن ارائه نشده‌اند. npm هنگام نصب ۱۰ آسیب‌پذیری در وابستگی‌ها گزارش کرد؛ ارتقای نسخه‌ها در این نصب انجام نشده است.
